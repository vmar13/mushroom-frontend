import React from 'react'

const Source = (props) => {

    const { citation } = props

        return(
            <div>
                <p>{citation}</p>  
            </div>
        )
    }

export default Source